type Book = {
  title: string
  author: string
}

type BookArgs = {
  id: number
}

type BookAuthorObj = {
  author: string
}

type AuthorObj = {
  firstName: string
  lastName: string
}

export { Book, BookArgs, BookAuthorObj, AuthorObj }
